.result
= form_tag collect_page_url,
           :class => 'remote-resource' do

  = label_tag :url,
              '请输入网页url'

  = text_field_tag :url, nil,
                   :class => 'url-field'

  = submit_tag
